# Backend REST API base URL
# Optional overrides. Leave blank to use the same origin (/api), which works
# with the Vite development proxy and the production Nginx container.
VITE_API_BASE_URL=

# Backend WebSocket URL
VITE_WS_URL=
